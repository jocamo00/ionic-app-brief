import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-app-brief',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
